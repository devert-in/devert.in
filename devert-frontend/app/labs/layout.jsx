export const metadata = {
  title: "Labs",
  robots: { index: false, follow: true },
  alternates: { canonical: "https://devert.in/labs" },
};

export default function LabsLayout({ children }) {
  return children;
}
