export const metadata = {
  title: "Login",
  description: "Sign in to DeVert - the builder's OS. Join the top 1% of developers.",
  alternates: { canonical: "https://devert.in/login" },
};

export default function LoginLayout({ children }) {
  return children;
}
