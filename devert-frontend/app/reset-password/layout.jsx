export const metadata = {
  title: "Set Your Password",
  description: "Set or reset your DeVert Campus password.",
  robots: { index: false, follow: false },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
