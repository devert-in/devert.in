export const metadata = {
  title: "Stories",
  robots: { index: false, follow: true },
  alternates: { canonical: "https://devert.in/stories" },
};

export default function StoriesLayout({ children }) {
  return children;
}
