# =====================
# Logs
# =====================
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*

# =====================
# Firebase
# =====================
.firebase/
# .firebaserc   # keep commented for team use

# =====================
# Runtime data
# =====================
pids
*.pid
*.seed
*.pid.lock

# =====================
# Coverage & build tools
# =====================
lib-cov
coverage
.nyc_output
.grunt
bower_components
.lock-wscript
build/Release

# =====================
# Node / Next.js
# =====================
node_modules/
.next/
out/
.npm
.eslintcache
.node_repl_history
.yarn-integrity
*.tgz

# =====================
# Environment files
# =====================
.env
.env.local
# functions/.env holds only DEVERT_BACKEND_URL - a public Cloud Run URL, not a
# secret (it's already plaintext in the CI workflow files) - Firebase's own
# functions deploy tooling reads this file to set the live env var, so it has
# to actually ship in the repo/CI checkout, unlike every other .env above.
!functions/.env

# =====================
# Secrets / service accounts
# =====================
scripts/service-account.json
*-service-account.json
serviceAccountKey.json

# =====================
# Maven / Java
# =====================
devert-backend/target/
*.class

# =====================
# OS files
# =====================
.DS_Store
Thumbs.db
