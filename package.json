{
  "type": "module",
  "scripts": {
    "build": "npm --prefix devert-frontend run build",
    "sync:shells": "node scripts/sync-static-shells.mjs",
    "deploy": "npm --prefix devert-frontend run build && npm run sync:shells && npx firebase-tools deploy --only hosting,firestore:rules,storage,functions --project devert-me",
    "deploy:rules": "npx firebase-tools deploy --only firestore:rules,storage --project devert-me",
    "deploy:hosting": "npm --prefix devert-frontend run build && npm run sync:shells && npx firebase-tools deploy --only hosting,functions --project devert-me",
    "upload:resources": "node scripts/upload-intel-resources.mjs",
    "test": "npx firebase-tools emulators:exec --only firestore \"node --test test/\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-admin": "^12.7.0"
  },
  "dependencies": {
    "simple-icons": "^16.27.0"
  }
}
