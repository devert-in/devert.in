{
  "type": "module",
  "scripts": {
    "build": "npm --prefix devert-frontend run build",
    "deploy": "npm --prefix devert-frontend run build && npx firebase-tools deploy --only hosting,firestore:rules --project devert-me",
    "deploy:rules": "npx firebase-tools deploy --only firestore:rules --project devert-me",
    "deploy:hosting": "npm --prefix devert-frontend run build && npx firebase-tools deploy --only hosting --project devert-me",
    "upload:resources": "node scripts/upload-intel-resources.mjs"
  },
  "devDependencies": {
    "firebase-admin": "^12.7.0"
  }
}
